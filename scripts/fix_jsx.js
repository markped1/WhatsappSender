const fs=require('fs');let c=fs.readFileSync('src/renderer/src/App.tsx','utf8');const fixes=[['className={w-1 h-1 rounded-full } />','className={w-1 h-1 rounded-full } />'],['className={w-8  border-r  flex flex-col items-center py-2 gap-3 shrink-0 z-20}','className={w-8  border-r  flex flex-col items-center py-2 gap-3 shrink-0 z-20}'],['className={p-1 rounded }><LayoutDashboard','className={p-1 rounded }><LayoutDashboard'],['className={p-1 rounded }><UserPlus','className={p-1 rounded }><UserPlus'],['className={p-1 rounded }><History','className={p-1 rounded }><History'],['className={p-1 rounded }><BookOpen','className={p-1 rounded }><BookOpen'],['className={p-1 rounded }><Ban','className={p-1 rounded }><Ban'],['className={p-1 rounded }><Settings','className={p-1 rounded }><Settings']];fixes.forEach(([a,b])=>{c=c.split(a).join(b)});fs.writeFileSync('src/renderer/src/App.tsx',c);console.log('Fixed',fixes.length,'items');
